{
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "start": "python -m uvicorn app.main:app --host 127.0.0.1 --port 8000",
    "setup": "python -m pip install -r requirements-core.txt"
  }
}
