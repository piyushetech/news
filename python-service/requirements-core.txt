# Core deps — enough to run scrape + enrich API (install first on Windows)
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
httpx>=0.28.0
feedparser>=6.0.11
beautifulsoup4>=4.12.0
lxml>=5.3.0
structlog>=24.4.0
prometheus-client>=0.21.0

# Optional — recommendations endpoint
motor>=3.6.0
pymongo>=4.10.0

# Optional — full AI stack (heavy; install separately)
# openai sentence-transformers spacy scikit-learn pandas numpy celery redis
